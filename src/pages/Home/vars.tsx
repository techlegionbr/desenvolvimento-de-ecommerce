import { colors } from "../../styles/vars";

export const sectionBackgroundColors = {
  header: "#111746e2",
  main: colors.primary,
  benefits: colors.primary_weak,
  techologies: colors.primary,
  stepBy: colors.primary_weak,
  socialInpact: colors.primary,
  faq: colors.primary_weak,
  formFooter: colors.primary,
  footer: colors.primary_weak
}